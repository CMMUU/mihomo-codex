# Mihomo Desktop 0.3.0 运行验证记录

[[index|返回文档索引]] | [[软件设计说明书|软件设计说明书（SDD）]]

## 执行摘要

| 项目 | 结果 |
|---|---|
| 执行日期 | 2026-08-23 |
| 本机平台 | macOS aarch64 |
| 应用版本 | 0.3.0 |
| Mihomo 版本 | 1.19.30 |
| Rust tests | 34 passed，1 ignored external fixture |
| TypeScript／Vite | BUILD SUCCESS |
| cargo fmt | PASS |
| cargo clippy | PASS，`-D warnings` |
| npm audit | 0 vulnerabilities |
| Tauri bundle | app 与 DMG 成功 |

## 自动化命令

```bash
npm run build
npm run test:core
cargo fmt --manifest-path src-tauri/Cargo.toml -- --check
cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings
cargo test --manifest-path src-tauri/Cargo.toml
npm audit --registry=https://registry.npmjs.org --audit-level=high
npm run tauri build
```

## 核心覆盖

| 模块 | 结果 |
|---|---|
| YAML 结构解析 | PASS |
| Effective 配置本机字段覆盖 | PASS |
| TUN 仅由本机模式启用 | PASS |
| 订阅 URL 策略 | PASS |
| 本地 HTTP 订阅与元数据 | PASS |
| UUID 版本存储与回滚 | PASS |
| 日志 URL／UUID／secret 脱敏 | PASS |
| 端口占用预检 | PASS |
| macOS networksetup 输出解析 | PASS |
| Mihomo `-v`／`-t` 冒烟 | PASS |
| OpenAI 托管组与优先规则注入 | PASS |
| 元数据伪节点过滤 | PASS |
| 隔离带宽 listener 生成 | PASS |
| OpenAI 节点评分与服务器维度分散 | PASS |
| 策略 revision 快照与回滚 | PASS |
| 订阅状态伪节点从 effective 代理和自动组成员过滤 | PASS |
| 自动组 HTTPS 204 健康检查规范化 | PASS |
| macOS 默认路由接口解析与 link-local 排除 | PASS |
| 当前节点递归链路、字段白名单与服务器脱敏 | PASS |
| 备用订阅 revision 更新不切换全局活动档案 | PASS |
| 本地代理三目标安全预检 | PASS |
| OS 物理接口与虚拟接口过滤 | PASS |
| 流量字节增量按采样时间归一化 | PASS |
| 菜单栏紧凑速率格式 | PASS |
| schema v2 设置默认迁移为开启流量监控 | PASS |
| 全局 App Shell 与独立页面滚动容器 | PASS |
| 顶部 System Proxy／TUN 状态同步与切换 | PASS |
| 非活动订阅 OpenAI 容灾 revision 写入 | PASS |
| 非活动订阅更新不切换活动档案或重载核心 | PASS |

## 应用端到端结果

1. 应用启动后识别内置 Mihomo 1.19.30。
2. 已有活动档案和 revision 在重启后恢复。
3. mixed port 7890 被 Clash Verge 占用时，启动前返回明确端口冲突。
4. 修改为 7895 后启动成功。
5. 监听端口为 `127.0.0.1:7895` 和 `127.0.0.1:9090`。
6. 经本地 HTTP 代理请求 `https://www.gstatic.com/generate_204` 返回 HTTP 204。
7. Mihomo API `/version` 返回 `v1.19.30`。
8. 代理组、规则、连接、日志和诊断页面可读。
9. 停止后 7895／9090 端口均关闭，无遗留 Mihomo 子进程。
10. 关闭窗口后应用保留于托盘；再次打开不会创建第二实例。
11. 主页显示 System Proxy、TUN 和 Global／Rule／Direct 控制。
12. 运行中从 Global 切换到 Rule 后，Mihomo `/configs` 返回 `rule`。
13. 深紫色主题、毛玻璃层、32～50 px 背景模糊与高斯光晕完成 1162×768 视觉验收。
14. 用户提供的订阅完成实际导入：54 个节点、3 个代理组、517 条规则。
15. 订阅导入后 Mihomo 启动成功，本地代理请求返回 HTTP 204。
16. 首次核心校验因下载 GeoIP／GeoSite 数据耗时约 1～2 分钟；界面现已显示持续加载状态并阻止重复提交。
17. OpenAI 最终筛选从 54 个原始代理条目中排除 4 个订阅状态伪节点，对 50 个候选执行检测，17 个通过 OpenAI 可达性检查。
18. 最终生成 10 个节点引用；其中 9 个取得带宽数据，实测范围为 2.61～6.86 Mbps，延迟范围为 901～2556 ms。该数据是 2026-08-19 当前网络快照。
19. 运行时 `/proxies` 返回托管组类型 `Fallback`、成员 10、`expectedStatus: 401`、无固定节点。
20. `/rules` 返回 14 条 OpenAI 托管规则，首条为 `DomainSuffix,openai.com`。
21. 经本地 mixed port 访问 OpenAI Models API 返回 HTTP 401，证明请求到达需要认证的官方端点。
22. 最终健康检查有 9／10 个成员响应，Fallback 当前节点为 alive；全部成员的历史选取结果仍保留用于下次检查。
23. 受控独立配置将第一节点指向本机不可达端口后，Fallback 自动跳到第二个健康节点，随后 OpenAI 请求返回 HTTP 401。
24. 0.3.0 最终构建执行配置热加载返回 HTTP 204，热加载前后主 Mihomo PID 均为 25341；无需重启主核心。
25. 取消检测后，临时核心进程和临时目录均为 0；完整任务结束后的 benchmark 目录同样清理完成。
26. 最终代理页同时展示新增 OpenAI 托管卡片以及原有 GLOBAL、宝可梦、故障转移、自动选择代理组；原节点选择和测速区域保留。
27. Figma 文件建立 3 个变量集合、58 个变量、7 个文字样式、2 个玻璃效果样式和 4 类本地组件。
28. Figma 最终 Screen 为 1180×780；字体审计只有 Inter，缺失字体 0、未命名图层 0。
29. 最终 Screen 复用 11 个 Button、8 个 Nav Item、4 个 Metric Cell 和 4 个 Proxy Group Card 实例。
30. 已读取 Figma node `8:2` Design Context，并转换为项目现有原生 TypeScript/CSS；未引入 React 或 Tailwind。
31. 本地浏览器验证代理导航后 `aria-current=page`，计算背景为 `rgba(81, 45, 120, 0.54)`，其余导航背景透明。
32. Figma 新增 `04 App Icon` 页面，主图节点为 `12:6`，独立导出节点为 `15:2`。
33. 图标完成 1024／512／128／32 px 验收；32 px 下 M 路由标志仍可识别。
34. 正式图标源为 1024×1024 RGBA，alpha 范围 0～255，四角 alpha 为 0。
35. Tauri 图标生成器已更新 ICNS、ICO、Linux PNG 和 Windows Store 全部桌面尺寸。
36. 打包后 `Contents/Resources/icon.icns` 与源码 ICNS SHA-256 完全一致。
37. macOS 应用完成 ad-hoc 深度签名；`codesign --verify --deep --strict` 返回 valid on disk，Sealed Resources 为 version 2。
38. 托盘菜单已禁用左键弹出；左键事件使用 `MouseButton::Left + MouseButtonState::Up` 调用 `show_home_window`。
39. 等价交互验收：窗口停留“规则”页并隐藏后，单实例唤醒复用同一调用链，窗口重新显示且标题切换为“概览”。
40. Figma 新增订阅管理 Screen `17:70` 与节点详情弹窗 `24:174`，原代理 Screen `8:2` 保留并新增“当前节点”入口。
41. 正式 Tauri 应用读取 3 个既有远程订阅；页面只显示 host 和脱敏 URI，不回显 path、query 或 token。
42. Manual 模式启动后，页面“安全检查”实际返回 Google 204、Cloudflare 204、OpenAI 401。
43. 代理页真实运行数据中，故障转移与自动选择当前值均为实际代理节点，订阅流量／到期状态伪节点不再进入 effective 组成员。
44. 当前节点弹窗从真实 OpenAI Fallback 读取生效链、VLESS、TLS、Provider、脱敏服务器、端口和延迟历史；“重新测速”后延迟历史由 3 条增加到 4 条。
45. 首次受控网络测试发现闲置 USB 网卡的 `fe80::` 会造成误判；实现已改为默认路由接口优先，并新增 link-local 排除测试。
46. 修复后默认路由为 `en0`，映射网络服务为 Wi-Fi；HTTP、HTTPS、SOCKS 读回均为 `127.0.0.1:7895` 且 enabled。
47. Wi-Fi System Proxy 接管期间，系统代理发现结果包含 HTTP／HTTPS，经系统代理访问 Google、Cloudflare、OpenAI 分别返回 204、204、401。
48. System Proxy 验收由 25 秒独立看门狗保护；测试结束后 HTTP／HTTPS／SOCKS 状态与测试前快照逐项相等，Wi-Fi 三类代理均恢复为 disabled。
49. 本地浏览器和正式 Tauri WebView 均验证“订阅”导航、导入表单、“当前节点”入口；正式应用节点详情弹窗完成视觉和 AX 结构验收。
50. 存储回归测试验证备用订阅更新自身 revision 后，全局 `activeProfileId` 和 `runtime/active.yaml` 均保持原活动档案。
51. Figma 新增 `Global Traffic / Sidebar` 组件 `27:3` 和 `Global Traffic / Menu Bar` 组件 `27:15`，并读取 Design Context 对照原生 TypeScript／CSS／Rust 实现。
52. Mihomo 为停止状态、系统代理未接管时，应用内组件仍实时显示 OS 全局流量；实机观测上传 8.1 KB/s、下载 27.2 KB/s。
53. macOS 菜单栏按最终要求改为统一黑白模板：M、箭头和 SFNS 抗锯齿数字在明亮菜单栏显示黑色，在深色菜单栏自动显示白色；上传在上、下载在下。
54. `showGlobalTraffic=false` 重启验收：应用内流量区域隐藏，菜单栏恢复静态紫色应用图标，Mihomo 运行状态仍保留。
55. 设置恢复 `showGlobalTraffic=true`，schema 保持 v3，最终发布状态默认为开启。
56. Mihomo Manual 运行中保存流量监控开关不再触发网络设置冲突；关闭后立即隐藏，重新开启后先建立基线，下一采样周期恢复实时速率。
57. 菜单栏上传／下载值在 Figma 改为 Semi Bold，Rust SFNS 栅格化结果增加 1 px 横向字形加粗，原生尺寸下数字辨识度提升且组件宽度保持紧凑。
58. 正式 Tauri WebView 验证删除交互已由无响应的 `window.confirm` 替换为应用内确认弹窗；活动订阅点击删除会在右上角提示先激活其他订阅。
59. 使用临时本地档案完成真实删除验收：点击“删除”→“确认删除”后目录被移除、档案数由 6 恢复为 5、活动档案 ID 保持不变，窗口右上角显示“订阅删除成功”。
60. 使用 CoreGraphics 原生像素滚轮事件并将光标置于订阅卡片上测试：订阅列表与页面统一滚动，不再出现内层 470 px 滚动区或滚轮接力跳变。
61. 订阅页面滚动后切换“概览”再返回，AX 标题坐标在切换前后均为 `(547, 172)`，证明分页面滚动位置恢复生效。
62. 删除确认弹窗打开期间注入 700 px 滚轮事件，关闭弹窗前后底层 AX 标题坐标均为 `(547, 172)`，证明背景滚动已锁定。
63. 窗口压力尺寸 `900×640` 验收通过：订阅列表与导入区域改为单列自然流，标题栏不吸顶遮挡内容，仍由主内容区统一滚动。
64. 全部菜单页改用同一个“应用状态” App Shell 顶栏，下方 `page-scroll` 独立滚动；在订阅页滚动到中部后，应用状态、System Proxy、TUN、刷新、启动和停止操作仍保持置顶可用。
65. 顶栏 System Proxy／TUN 与概览页共用同一状态源和切换流程；切换期间锁定重复操作，停止状态下可按选定模式直接启动。
66. 每个订阅卡片均显示“OpenAI 容灾／重新生成容灾”操作；全局仅允许一个生成任务，当前卡片显示进度与取消，其他卡片在任务期间禁用。
67. 对真实备用订阅执行检测时，50 个候选节点中 0 个通过当次 OpenAI 可达性检查；页面明确显示失败原因，原 revision 未被替换。
68. 受控备用订阅使用 3 个可用 SOCKS5 节点完成生成：revision 数由 1 增加到 2，OpenAI 策略启用，候选 3／3 全部通过。
69. 上述备用订阅生成前后，全局活动档案 ID、活动 revision、主 Mihomo PID 和系统代理状态均未变；生效 YAML 已包含 Fallback 组与 OpenAI／ChatGPT 优先规则。
70. 联网切换验收使用临时隔离代理保持对话链路，最终安装包启动后再交接回 `127.0.0.1:7895`，没有在应用未就绪时切断系统联网。
71. 最终应用 PID 为 80988，Mihomo PID 为 85045；Wi-Fi HTTP／HTTPS／SOCKS 均已接管到 `127.0.0.1:7895`，Google generate_204 返回 204，OpenAI Models 返回预期的未认证 401。

## 发布产物

| 产物 | 路径／值 |
|---|---|
| DMG | `src-tauri/target/release/bundle/dmg/Mihomo Desktop_0.3.0_aarch64.dmg` |
| DMG SHA-256 | `49b29bad4fabec433f1684ed1f2ec673a3082cc3826ba09ea2bd08987750a3b9` |
| DMG 大小 | 23.5 MiB |
| app 大小 | 61 MiB |
| Mihomo SHA-256 | `e80c6334b4e3aae53dfbc86cddd4434cec1565a61d4483931fac2ae12fec6d30` |
| 未签名发布二进制 SHA-256 | `e9a18ca89497581ecc99bf89bf3fa0d35f60e155fb2374e01d4e4797aafbf23a` |
| app 内已签名主二进制 SHA-256 | `f4485a7890b90192d2093bf484533c68f9b81d109e75373fff55d5379a06d66c` |
| Figma Icon | `https://www.figma.com/design/aqVzL0f9upkr8BiYNCy2fu?node-id=12-3` |
| Figma Icon PNG SHA-256 | `b8be568fb41a213e274fa2f0eb77b24b1f0fb17a0126ca5e65b91774799e6477` |
| ICNS SHA-256 | `61da6c8c317cf2951af439b590bd2fe5f9fa70123584aa16aed933e83954f47b` |
| ICO SHA-256 | `cb03bddf878b4a7d37a60fdb1576e7871d7cadf9d90a000001a2f17ffe8c9cc1` |
| Figma Screen | `https://www.figma.com/design/aqVzL0f9upkr8BiYNCy2fu?node-id=8-2` |
| UI 验收截图 | `/opt/workspace/codex/artifacts/mihomo-desktop/figma/Implemented-Proxy-Screen.jpeg` |
| 订阅管理实机截图 | `/opt/workspace/codex/artifacts/mihomo-desktop/final/subscriptions.png` |
| 删除确认弹窗实机截图 | `/opt/workspace/codex/artifacts/mihomo-desktop/debug-delete/fixed-confirm-modal.png` |
| 删除成功实机截图 | `/opt/workspace/codex/artifacts/mihomo-desktop/debug-delete/temp-profile-delete-success.png` |
| 统一滚轮实机截图 | `/opt/workspace/codex/artifacts/mihomo-desktop/scroll-ux/final-wheel-down.png` |
| 滚动位置恢复截图 | `/opt/workspace/codex/artifacts/mihomo-desktop/scroll-ux/final-position-restored.png` |
| `900×640` 压力视口截图 | `/opt/workspace/codex/artifacts/mihomo-desktop/scroll-ux/final-min-900x640.png` |
| 节点详情实机截图 | `/opt/workspace/codex/artifacts/mihomo-desktop/final/node-details.png` |
| 流量监控实机截图 | `/opt/workspace/codex/artifacts/mihomo-desktop/traffic-monitor/final-enabled.png` |
| macOS 菜单栏实机截图 | `/opt/workspace/codex/artifacts/mihomo-desktop/traffic-monitor/final-menubar.png` |
| 关闭监控实机截图 | `/opt/workspace/codex/artifacts/mihomo-desktop/traffic-monitor/final-disabled.png` |
| Figma 应用内流量组件 | `https://www.figma.com/design/aqVzL0f9upkr8BiYNCy2fu?node-id=27-3` |
| Figma 菜单栏流量组件 | `https://www.figma.com/design/aqVzL0f9upkr8BiYNCy2fu?node-id=27-15` |
| 应用状态顶栏与运行实机截图 | `/opt/workspace/codex/artifacts/mihomo-desktop/app-status-openai/final-running-overview.png` |
| 订阅页固定顶栏滚动验收 | `/opt/workspace/codex/artifacts/mihomo-desktop/app-status-openai/smoke-fixed-header-scrolled-real.png` |
| 逐订阅 OpenAI 容灾入口截图 | `/opt/workspace/codex/artifacts/mihomo-desktop/app-status-openai/smoke-subscriptions.png` |
| 真实备用订阅失败反馈 | `/opt/workspace/codex/artifacts/mihomo-desktop/app-status-openai/smoke-standby-generation-failed.png` |
| 受控备用订阅生成结果 | `/opt/workspace/codex/artifacts/mihomo-desktop/app-status-openai/smoke-controlled-generation-status.png` |

发布包内已包含：

- Mihomo sidecar。
- 当时包含误标为 MIT 的 Mihomo License 文件。更正：2026-09-03 已核验官方 v1.19.30 为 GPL v3；首个 GitHub Release 前已替换许可证并重新打包，见 [[发布说明-v0.3.1|v0.3.1 发布说明]]。
- THIRD_PARTY_NOTICES.md。

## 跨平台边界

- 六个目标平台 Mihomo 压缩资产已经过固定 SHA-256、解压和命名验证。
- GitHub Actions 使用 macOS ARM64／Intel、Windows x64／ARM64、Linux x64／ARM64 原生 runner。
- 当前本机的 Windows cross-check 被本地缺失 Windows MSVC C SDK 阻塞；平台代码由原生 Windows runner 验证。
- System Proxy 与 TUN 尚需在各目标平台执行真实网络设置、权限、恢复和休眠回归。
